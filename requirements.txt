fastapi
uvicorn[standard]
pydantic
openai
google-genai
anthropic
cryptography
faster-whisper
python-multipart
edge-tts
psutil
pywebpush
icalendar
# optional: real always-on wakeword detection (JARVIS_WAKEWORD_ENGINE=openwakeword).
# Needs system audio libs first — on Debian/Ubuntu: apt-get install portaudio19-dev
#   pip install openwakeword pyaudio
# openwakeword
# pyaudio
