{
  "tasks": [
    {
      "id": "win-midnight",
      "prompt": "In scripts/agent/autonomy_loop.py: stelle sicher, dass within_allowed_windows ein Zeitfenster ueber Mitternacht (z. B. 22:00-06:00) korrekt behandelt, und ergaenze einen Test in tests/test_autonomy_loop.py.",
      "check": "python3 -m pytest -q tests/test_autonomy_loop.py -k within_allowed_windows"
    },
    {
      "id": "task-escalation",
      "prompt": "In jarvis/autonomy_task_store.py: sorge dafuer, dass eine Aufgabe nach zwei erfolglosen Versuchen als escalated markiert und von next_open_task uebersprungen wird (Test in tests/test_autonomy_task_store.py).",
      "check": "python3 -m pytest -q tests/test_autonomy_task_store.py -k escalat"
    },
    {
      "id": "patch-protected",
      "prompt": "In jarvis/github_gateway.py: stelle sicher, dass validate_patch geschuetzte Pfade (AGENTS.md, deploy/**) und Binaerpatches ablehnt; Test in tests/test_github_patch.py.",
      "check": "python3 -m pytest -q tests/test_github_patch.py -k validate"
    },
    {
      "id": "briefing-summary",
      "prompt": "In jarvis/assistant_domain.py: haenge an das Briefing eine kurze Agenten-Zusammenfassung aus autonomy_task_store.daily_summary(), ohne dass ein defekter Store das Briefing bricht (Test in tests/test_autonomy_briefing.py).",
      "check": "python3 -m pytest -q tests/test_autonomy_briefing.py"
    },
    {
      "id": "dedup-area",
      "prompt": "In jarvis/autonomy_task_store.py: next_open_task soll Aufgaben in exclude_areas ueberspringen; Test in tests/test_autonomy_task_store.py.",
      "check": "python3 -m pytest -q tests/test_autonomy_task_store.py -k excludes"
    },
    {
      "id": "usage-metrics",
      "prompt": "In jarvis/autonomy_task_store.py: implementiere record_metrics/aggregate_metrics fuer Rundenmetriken (gpu_seconds, Tokens, Kosten) mit Tests in tests/test_autonomy_task_store.py.",
      "check": "python3 -m pytest -q tests/test_autonomy_task_store.py -k metrics"
    }
  ]
}
